# Base URL of the AccessPath FastAPI backend. Copy to .env for local overrides;
# docker-compose.yml sets this to the api container's address for the
# containerized dev setup.
VITE_API_BASE_URL=http://localhost:8000
